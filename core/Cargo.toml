[package]
name = "core"
version = "0.1.0"
edition = "2024"

[dependencies]
# For Linux input control
evdev = "0.12.1"
# For ultra-lightweight face detection
opencv = { version = "0.92", default-features = false, features = ["videoio", "objdetect", "imgproc"] }
